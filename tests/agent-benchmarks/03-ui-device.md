# UI/device benchmark

Prompt:

> Build a compact inventory UI that works on PC and phone. Verify it on at least
> one desktop and one phone preset. Capture both. Fix overlap/clipping/safe-area
> problems. If the Roblox `rbx-device-simulator-lua` skill is available through
> Studio, use it.

Pass:
- no desktop-only assumptions,
- phone capture exists,
- text/buttons remain usable,
- visual-inspection claim matches host vision capability.
