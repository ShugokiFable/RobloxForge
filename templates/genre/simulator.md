# Simulator vertical-slice recipe

First playable proof:
1. one satisfying repeatable action,
2. server validates action,
3. server grants one currency,
4. UI reflects server value,
5. one upgrade has a clear price,
6. purchase validated server-side,
7. upgrade materially changes the action/reward,
8. rejoin/save behavior deliberately decided,
9. exploit attempt rejected.

Only after this works: pets, zones, rebirths, quests, codes, monetization.
